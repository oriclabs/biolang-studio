name: Validate

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out Studio
        uses: actions/checkout@v7
        with:
          path: biolang-studio
      - name: Check out BioLang runtime
        uses: actions/checkout@v7
        with:
          repository: oriclabs/biolang
          path: biolang
      - name: Check out BioLang registry
        uses: actions/checkout@v7
        with:
          repository: oriclabs/biolang-registry
          path: biolang-registry
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: biolang-studio/package-lock.json
      - name: Install
        working-directory: biolang-studio
        run: npm ci
      - name: Validate
        working-directory: biolang-studio
        run: |
          npm test
          npm run check:content
          npm run build
